Before running the server, you need to create an OpenNode dev API key: https://app.dev.opennode.com/signup. Add the API key to `main.go`.

To start the server, run `go run main.go` in the terminal and visit http://localhost:8080.
